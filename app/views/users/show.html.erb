<%= button_to "Create a new garden", "/users/gardens/new", method: :get%>
<br>
<% if current_user.gardens.any? %>
  <% @gardens.each do |garden| %>
    <%= garden.name %>
    <%= link_to "Add plants to garden", "/users/gardens/#{garden.id}/plants/new"%><br>
  <% end %>
<% end %>
